[pytest]
pythonpath = .
testpaths = tests
markers =
    companion: companion/personality modules (requires DRIFT_COMPANION_ENABLED)
    stress: long-running or load tests
    env: requires external services or heavy optional deps
filterwarnings =
    ignore::DeprecationWarning
